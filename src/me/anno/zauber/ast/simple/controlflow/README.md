# Control Flow

This package contains classes defining control flow.

Flow is one kind of flow: normal execution | return value | thrown
FlowResult combined all three states into once to build three control flow graphs in one.

SimpleReturn, SimpleThrow, and SimpleYield are the different ways to exit a function.